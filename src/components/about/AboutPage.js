import React from "react";

//arrow function. We dont need to use the return key as its a single expression. We can use () instead of {}

const AboutPage = () => (
    <div>
        <h1>About Page</h1>
        <p>
            This App uses React, Redux, React Router, and many other helpful
            libraries
        </p>
    </div>
);

export default AboutPage;
